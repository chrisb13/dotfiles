Setting up for the first time!

Clone and install YCM with:
git clone --recurse-submodules https://cbull@bitbucket.org/cbull/dotfiles.git 
cd ~/.vim/home/chris/.vim/bundle/YouCompleteMe;./install.sh

Make links:
ln -s ~/.vim/sshconfig ~/.ssh/config
ln -s ~/.vim/vimrc ~/.vimrc 
ln -s ~/.vim/bashrc ~/.bashrc 
ln -s ~/.vim/tmux.conf ~/.tmux.conf
ln -s ~/.vim/gitconfig ~/.gitconfig 
ln -s ~/.vim/inputrc ~/.inputrc 
ln -s ~/.vim/liquidpromptrc ~/.liquidpromptrc
ln -s ~/.vim/i3config ~/.i3/config
ln -s ~/.vim/dircolors_light ~/.dircolors OR ln -s ~/.vim/dircolors_dark ~/.dircolors
ln -s ~/.vim/lxterminal ~/.config/lxterminal/lxterminal.conf
ln -s ~/.vim/screenrc ~/.screenrc
